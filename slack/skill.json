{
  "name": "slack",
  "version": "1.0.1",
  "description": "Send deploy notifications to Slack via webhook",
  "author": "ImJustRicky",
  "hooks": ["post-deploy", "post-rollback"],
  "requires": ["curl"],
  "config": [
    {
      "key": "MUSTER_SLACK_WEBHOOK",
      "label": "Slack Webhook URL",
      "hint": "Create one at https://api.slack.com/apps > Incoming Webhooks",
      "secret": true
    }
  ]
}
